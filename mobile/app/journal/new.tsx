// Standalone /journal/new route — re-exports the tab screen logic
// This allows navigating to /journal/new from anywhere in the app
import NewEntryScreen from '../(tabs)/new-entry';
export default NewEntryScreen;
